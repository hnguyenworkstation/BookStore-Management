@using BookStore_Management.Models;
@model List<ChuDe>
    @foreach (var item in Model)
    {
        <a href="#" class="button">@item.TenChuDe</a>
    }

<a href="#" class="button button-submit">More..</a>